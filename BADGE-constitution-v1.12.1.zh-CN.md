# BADGE 开发宪法 v1.12.1

> **B**oundary **A**nd **D**efensive **G**uard for **E**ngineering — 边界与守护工程开发宪法
>
> 好项目不是靠勤奋检查出来的，是靠设计让人根本没法犯错。
> 本宪法是工程品味的沉淀——它告诉你为什么这么设计是对的，不只是告诉你应该怎么做。
>
> **本宪法的三层结构：** 原则告诉你为什么这么做是对的，规则告诉你必须怎么做，技法告诉你具体怎么做。原则没有规则支撑，就成了空话。规则没有技法支撑，就不可执行。技法没有原则指引，就不知道为什么这么做。三者缺一不可。

---

# 第一层：设计哲学

## 一、边界思维

边界是系统设计的第一步。大到服务之间的网络边界，小到函数之间的参数边界，边界划在哪里，决定了系统的复杂度分布、可测试性、可扩展性。先划边界，再填实现。

### 1.1 模块边界：一事一责

每个模块只做一件事，把这件事做好。如果一个模块的职责无法用一句话说清楚，就该拆。模块内部可以很复杂，但模块之间的边界必须清晰简单。

**边界好的实际效果：** 修改上层代码时，不需要打开下层文件。比如一个 agent 主模块调用了十几个工具，你只需要知道每个工具的接口和作用，不需要看它们的实现代码。哪怕这些工具加起来有几千行，都和你修改 agent 调度逻辑没关系。这就是边界的力量——它把修改的影响范围限定在边界之内，让你可以安全地修改，不用担心中下游的连锁反应。

**判断标准：** 你能在不看代码的情况下，仅凭模块名和接口签名，准确说出它的职责吗？如果不行，边界没划清楚。修改一个模块时，你需要打开的下游文件数量是否趋近于零？如果不是，边界还需要加固。

### 1.2 接口边界：稳定抽象，可换实现

模块之间通过稳定的抽象接口通信，不依赖对方的具体实现。新增一个模型后端、新增一个采样策略、新增一个存储引擎——只需实现接口并注册，不修改任何现有代码。对扩展开放，对修改关闭。

**判断标准：** 新增一个实现需要改动几行现有代码？如果超过 0 行，接口边界需要重新设计。

### 1.3 层次边界：计算与设施分离

代码只有两种：一种是做**计算**的（算法、业务规则、数据变换），一种是做**设施**的（GPU 通信、网络 I/O、文件系统、数据库）。计算代码不依赖任何设施，可以在单线程本地跑、可以单独测试、可以在任何环境复现。设施代码负责把计算结果接入真实世界。

这个思想来自 Flink、Hadoop 等大数据分布式系统——不要让工程师一边操心业务逻辑，一边操心底层实现，一边操心基础设施。谁也不可能同时干好这三件事。

**判断标准：** 你能在不启动 GPU、不连网络、不读文件的情况下运行核心计算逻辑的测试吗？如果不行，层次没分离。

### 1.4 数据边界：单一真相源

每一条信息在系统中只有**一个**权威来源。配置只从一个文件读入，不搞环境变量 fallback 链。状态只在一个地方维护，不搞多副本同步。数据只沿一个方向流动，不搞回调、不搞循环依赖。

"这个值可以从三个地方来"听起来灵活，实际上用户改了参数发现没生效，debug 一下午。灵活性在这里不是优点，是 bug 的温床。

**判断标准：** 如果有人问"这个值是从哪来的"，你能在一秒内给出唯一答案吗？如果不能，源头太多了。

### 1.5 任务边界：分解即执行

模块边界约束代码结构，任务边界约束开发过程。同一个原则：**一个任务只做一件事，一件事做完再做下一件。**

面对复杂问题时，人的直觉是"一次性解决所有问题"，结果就是上下文膨胀、决策质量下降、中间状态不可验证。原则：复杂问题必须分解为边界清晰的独立子任务——每个子任务输入明确、输出可验证、不依赖其他子任务的内部状态。

**规则：** 一个子任务应能独立完成并产出可验证的结果。任务之间通过明确的输入输出接口通信，不共享隐式状态。调用方应能仅凭输出结果判断任务是否成功。

**技法（AI 辅助开发）：** 将大任务拆分为多个具体、单一职责的子任务，并行分派给 agent 执行，各自产出独立可验证的结果，最后汇总。效果：节省上下文窗口，获得更详细的产出，缩短总耗时，每个子任务的正确性可独立验证。

**判断标准：** 一个子任务能否在不依赖其他子任务上下文的情况下，独立完成并产出可验证的结果？如果不能，拆分还不够细。任务完成后，调用方能否仅凭输出结果判断任务是否成功？如果不能，输出边界不清晰。

### 1.6 文档边界：细节进注释，结构进文档

AI 时代，代码是第一文档——AI 直接读写代码，维护一份与代码同步的详细架构文档既昂贵又必然过期。架构文档和代码注释的分界，本身就是一条边界决策：

- **架构文档**：给人和 AI 看的。写模块边界、职责、接口，配架构图、流程图、数据流图、泳道图——看完了就知道系统是如何运作的。不含实现细节。
- **代码注释**：给 AI 看的。一是解释重点代码的逻辑和设计理由（为什么这么设计）；二是每个文件开头的职责声明——这个文件的功能、职责范围，给代码文件划清边界，防止 AI 修改时在文件里堆砌不相关的方法和类（这是防呆装置，见 §2）。

**判断标准：** 架构文档能否在不含实现细节的情况下讲清系统如何运作？如果不能，细节没有下沉到注释。文件头的职责声明能否一句话说清"这段代码该不该放进这个文件"？如果不能，文件边界没划清楚。

---

## 二、防呆设计

汽车设计有一个原则：踩死油门有转速限制，挂档有同步器，刹车优先于油门，车窗除雾按钮放在容易够到的地方。说明书几百页，没几个车主看过——但事故率很低，因为**系统不允许人犯错**。

软件也应该是这样。不是祈祷开发者小心、祈祷用户仔细读文档，而是用设计让错误根本不可能发生。

### 2.1 契约即防呆

所有边界上的交互都用**可验证的契约**来约束，不用约定和默契。pydantic 模型校验配置字段，ABC 抽象类强制子类实现钩子，类型标注让 mypy 检查数据流——这些不是"最佳实践"，是"防呆装置"。写代码的人不会出错，不是因为他认真，而是系统不允许他出错。

### 2.2 显式即防呆

代码里没有魔法。状态是显式的，数据流是可见的，依赖是声明的。不以字符串拼路径，不以 `**kwargs` 一路透传导致无法追踪参数来源，不以 `hasattr` 探测业务接口。隐式行为是理解代码的最大障碍——对人和 AI 都是。当一切显式，错误就无处藏身。

**`**kwargs` 的合理使用：** 在 ABC 抽象方法的适配器模式中，`**kwargs` 作为扩展点是允许的——不同子类可能需要不同的领域参数，基类不应该为每个子类的特殊需求而修改接口。关键是：`**kwargs` 必须在 docstring 中说明子类可能接受的额外参数，让调用者知道该传什么。HuggingFace 的 `model.generate(**kwargs)` 就是这种模式的标准范例。

**`hasattr` 的合理使用：** 以下三种场景允许使用 `hasattr`：
1. **运行时能力检测**：检查 PyTorch 版本是否支持某个特性（如 `hasattr(torch, "float8_e8m0fnu")`），这比解析版本号更可靠
2. **跨版本兼容性处理**：处理上游库（如 HuggingFace）不同版本返回不同类型的输出（`output.logits` vs `output["logits"]`）
3. **Duck-typing 类型判断**：在不引入硬依赖的前提下判断对象类型（如区分 tensor 和普通数值）

禁止的是用 `hasattr` 探测自己项目的业务接口——那意味着接口契约不清晰，应该用 ABC 或 Protocol 定义明确的接口。

### 2.3 边界校验即防呆

数据在跨边界时必须校验。配置加载时校验所有字段，拒绝未知字段，检查必填项。API 请求发出前校验格式，收到响应后校验结构。输出落盘前检查是否覆盖已有数据、是否泄露敏感信息。错误在边界上就被拦截，不让非法数据流入内部，不让敏感数据流出外部。

**一个核心区别：** 边界校验拒绝非法请求（如配置缺失），这不是"退路"，这是"防线"。防线的作用是阻断，退路的作用是绕行。不要混淆。

---

## 三、退路设计

退路是一条核心原则：**降级可以改变代价，不能改变结果。**

### 3.1 同效退路

系统可以自动执行，无需告知用户。结果不变，只改变代价——时间、资源、速度。

- 重试：多花了时间，最终拿到的结果是一样的。rollout 失败重试 5 次，全部失败才报错。
- 降速：网络拥塞时降低并发，虽然慢了，但数据完整到达。
- 备胎：限速 80km/h，但能把你送到目的地。

### 3.2 透明退路

结果可能受影响，系统必须明确告知用户。日志 WARNING 级别记录，metrics 暴露，让用户自己判断是否需要干预。

- 跳过损坏样本：日志记录样本 ID 和跳过原因，训练继续，但用户知道发生了什么。
- 使用缓存：数据过期但网络不可达时，使用过期缓存并告知用户。

### 3.3 预授权退路

结果会显著改变，用户必须在配置中**提前声明接受**，默认关闭。这不是运行时决策，是部署前决策。

符合此原则的预授权退路示例：
- 数据加载时跳过损坏的训练样本——仅当用户显式设置配置开关（默认关闭）时才可接受，因为模型将在与预期不同的数据集上训练。
- 覆盖已有输出目录——仅当用户显式选择接受（默认关闭）时才可接受，因为这会破坏之前的运行结果。

宪法不规定具体的配置键名——每个项目定义自己的开关。原则是：如果结果显著改变，用户必须在部署前说"可以"，而非事后才发现。

### 3.4 退路与防线的区别

密码过期不能登录？这不是退路，这是**防线**在起作用——身份验证的边界校验发现不合规，拒绝通过。配置字段缺失退出？这是防线。静默降级到更差的模型？这是**坏的退路**——它改变了结果，而且用户不知道。

---

## 四、零步上手

一个新用户 clone 项目后，两条命令就能看到产出结果。不需要额外安装系统依赖（都写在 Docker 里），不需要手动配置（默认值就是生产级合理值），不需要理解内部架构（example config 复制一份就能用）。

**零步不是说零配置，而是说默认配置就是可用配置。** 如果用户必须理解 10 个参数的含义才能开始，那就是零步的失败。

**判断标准：** 一个新用户从 clone 到看到第一个产出结果，需要几步操作？超过 3 步就该反思。

---

## 五、先正确，后可优化

性能优化是必要的，但不能以牺牲正确性和可维护性为代价。原则是：**先保证正确，再追求性能。但不要设计成无法优化的架构。**

计算与设施分离已经为性能优化提供了天然的基础——核心算法可以独立调优，不耦合到分布式通信或 I/O 中。接口的稳定抽象保证了替换低效实现时不影响上层。如果为了"代码优雅"而把多个本该独立执行的操作耦合在一起，导致后面想并行化时发现根本拆不开，这就是过度设计——它牺牲了优化空间，却什么都没换来。

**判断标准：** 如果发现某个组件是性能瓶颈，你能在不改动上层代码的情况下替换它吗？如果不能，架构需要重新审视。

---

## 六、环境可复现

项目在任何两台硬件相同的机器上运行，产出应该**比特级一致**（排除不可控随机性后，完整定义见 §10.1）。这不是理想，是可验证的工程标准。如果两次运行结果不同，要么是随机种子没固定，要么是依赖版本不一致，要么是代码里有非确定性操作——这些都应该被消除。不可控随机性（机器负载导致的耗时差异、GPU 内核选择等无法控制的差异）不属于复现破坏。

**实现要点：**
- 随机种子在配置文件中显式设定，不依赖系统时间或硬件状态
- `uv.lock` 锁定所有依赖的精确版本，提交到 git
- Docker base image 锁定具体 SHA256 digest（不是 tag，tag 可以被覆盖）
- 每次运行的配置自动备份到输出目录，确保事后可复现

**判断标准：** 两台机器、同一 config、同一 seed，产出是否一致？运行两次，产出是否一致？"一致"指排除不可控随机性后的落盘输出一致——判断参数归属时，以 §10.1 的复现定义为准。

---

# 第二层：工程规范

## 七、配置系统

### 7.1 单一 YAML 入口

所有配置在一个 YAML 文件中，按功能域分段。不使用环境变量（NVIDIA 的 `CUDA_VISIBLE_DEVICES`、`NCCL_*` 已经够乱了，不要再往里面加），不搞 CLI 参数覆盖配置文件的隐式优先级。配置就是配置，环境就是环境，泾渭分明。

### 7.2 加载即校验

配置加载时完成全部校验，不在使用点零星校验。pydantic 的 `extra="forbid"` 拒绝拼写错误的字段名，`model_validate()` 一次性检查所有字段的类型和约束。校验失败立即报错退出，给出明确的错误信息——哪个字段、期望什么、实际收到了什么。

### 7.3 模板即文档

项目提供 `config_example.yaml`，包含所有字段、注释和默认值。用户复制一份改几个值就能用。模板文件本身是一个可用的配置——注释充分、默认值合理，覆盖 80% 的使用场景。

### 7.4 配置参数设计

配置参数是用户和系统之间的边界。参数设计不好，文档写得再清楚也没用——用户会被参数名误导，会被冗余参数淹没，会被耦合参数困住。参数设计的三条原则：

**1. 不暴露无用参数。** 能自动推导的就不让用户手动指定。一个参数如果可以从其他参数唯一确定，就不应该存在——每多一个参数，用户就多一个犯错的机会。`total_steps` 可以从 `max_steps × optimize_iterations_per_step × rollout_group_size` 推算，就不要暴露给用户。`num_layers` 从配置文件加载就能知道，就不要让用户手动填。原则是：**用户只需要指定他真正关心的、系统无法替他决定的参数。**

**2. 参数正交。** 参数之间应相互独立，调整一个不影响另一个的语义。如果改了 `warmup_steps` 必须同时改 `total_steps` 才能让调度器正常工作，说明这两个参数耦合了，设计有问题。正交的配置让用户可以独立优化每个维度，不需要在脑子里维护一个"参数联动表"。

**判断标准：** 用户能否独立调整每个参数并得到可预测的结果？如果改参数 A 必须同时改参数 B，说明它们不正交，应该合并成一个参数，或者让系统自动推导其中一个。

**3. 参数名自解释。** 参数名可以长，但必须让用户一眼看懂含义，不需要查文档。`learning_rate` 比 `lr` 好，`warmup_steps` 比 `ws` 好，`min_lr_ratio` 比 `min_lr` 好（后者让人以为是绝对 LR 值而不是比例）。缩写只在一种情况下允许：该缩写是领域内**无人不知**的通用术语，如 `lr`、`tp`、`pp`、`lora`。但即使这些术语，在配置文件中作为顶层参数名时也建议使用全称——`learning_rate` 比 `lr` 更不易产生歧义。

**判断标准：** 找一个不熟悉项目的人，只看参数名和注释，他能否准确说出这个参数的作用？如果他能说对但说错单位或范围，说明参数名对但注释不够。如果他说错作用，说明参数名本身有歧义。

---

## 八、代码组织

### 8.1 目录布局

```
project/
├── src/project_name/        # 源码，src-layout
│   ├── __init__.py          # 版本号 + 公开 API re-export
│   ├── __main__.py          # python -m 入口
│   ├── py.typed             # PEP 561 标记
│   ├── cli.py               # CLI 入口（复杂时可升级为 cli/ 目录）
│   ├── config.py            # 配置模型定义与加载
│   ├── core/                # 计算层：纯逻辑，零设施依赖
│   ├── backends/            # 设施层：分布式、GPU、网络
│   └── domain/              # 领域逻辑层（按需存在）
├── configs/                 # 示例配置
├── docs/                    # 架构文档
├── tests/                   # 测试（目录树镜像源码）
│   ├── core/
│   ├── backends/
│   └── e2e/                 # 端到端测试
├── scripts/                 # 一次性工具脚本
├── .local/                  # 临时本地文件（永久不入库，见 §XVI）
├── docker/                  # Docker 构建
├── pyproject.toml
├── uv.lock
├── .python-version
├── README.md
├── README.zh-CN.md
├── LICENSE
└── .gitignore
```

**目录组织原则：** 目录结构是代码库的导航系统，不是文件收纳箱。通过目录层级和文件名，读者应能大致了解代码的作用、所属模块和继承关系——无需打开文件。一个目录中的代码文件不宜超过 10 个。超过时，说明目录内存在多个可独立命名的子域——建分类子目录，按功能域归纳。这不是硬性上限，而是信号：当你看到第 11 个文件时，问自己"这个目录的职责是否仍然能用一句话说清"。

**`cli.py` 可升级为 `cli/` 目录：** 当 CLI 逻辑复杂到需要拆分为多个子模块（如子命令分发、训练 worker 进程入口），遵循与 §8.3（类改目录）相同的逻辑——`cli/__init__.py` 负责对外透明，外部使用者只看到 `graspo.cli:main` 入口点，不感知内部是单文件还是目录。

**`domain/` 可按需省略：** 不是所有项目都有独立的领域逻辑层。如果项目的领域逻辑自然内聚在 `core/` 的计算模块中，或者领域概念本身尚未稳定到足以独立分层，空着比硬塞好。模块边界（§1.1）的核心原则是用一句话说清楚职责——如果说不清楚 `domain/` 的职责是什么，它就不应该存在。

### 8.2 文件粒度：既不大也不碎

一个文件大到几千行，阅读和修改都困难，IDE 打开都慢。拆得太碎，一个功能散落在十几个文件里，读者到处跳转，思维被割裂。理想的粒度是：**一个文件对应一个明确的概念单元**，读者打开这个文件就能完整理解这个概念。

判断标准：你能在一个文件里完整理解一个概念吗？如果需要在多个文件间反复跳转才能拼出全貌，太碎了。**建议文件不超过 1000 行**——如果超过，问问自己它是不是塞进了两个概念。但如果文件内的逻辑确实属于同一概念单元（如纯函数工具集、复杂模型适配器），超过 1000 行是可以接受的；此时应确保文件内部按功能域用清晰的分隔线和注释组织，让读者能快速定位。

### 8.3 类改目录：当类太大时

当一个类的方法多到让单个文件膨胀到几百行甚至上千行，不应该把方法硬塞进一个文件，而应该把类升级为一个目录：

```
backends/models/qwen35_36/
├── __init__.py          # 从 adapter.py 重导出 Qwen35Adapter
├── adapter.py           # 主类定义 + 模板方法骨架
├── forward.py           # 前向传播相关的方法
├── generation.py        # 生成/采样相关的方法
├── checkpoint.py        # checkpoint 存取相关的方法
└── helpers.py           # 纯函数工具（不依赖 self 状态）
```

**原则：** `adapter.py` 保留类的骨架——`__init__`、模板方法、抽象方法。按功能域拆出去的每个文件，包含该类的一组相关方法。外部使用者只 import 类名，完全不感知内部是单文件还是目录。`__init__.py` 负责这个"对外透明"。

内部方法之间如果需要共享状态，通过 `self` 访问——它们仍然是同一个类的方法，只是物理上分散在不同的文件中。不需要 `self` 状态的纯函数放进 `helpers.py`，它们是独立的、可单独测试的。

类目录内的文件名不必与类名一致——目录名承载域（类名主体）的定位，内部文件按功能域命名（§12.2 类名与路径互映）。

### 8.4 OOP 与 FP 各司其职

面向对象和函数式编程争论了几十年谁更好。答案是：**它们不是对手，是工具。** 关键是让它们各自做自己擅长的事。

**OOP 擅长：** 有状态的、有生命周期的、有多套实现需要统一接口的场景。类继承表达"同一类事物，不同实现方式"——模型后端、采样策略、调度策略。模板方法让基类控制流程，子类填充差异。

**FP 擅长：** 无状态的、纯计算的、数据变换的场景。函数接收输入，返回输出，无副作用。奖励计算、数据清洗、格式转换、文本解析——这些用纯函数最自然，输入输出一目了然，测试不依赖任何上下文。

**不要为了 OOP 而 OOP：** 如果一段逻辑不需要 `self` 状态，不要强行放进类里。一个独立的纯函数比一个只有一个方法的类更清晰、更容易测试、更容易复用。

**模块内的混用需要分级看待：**

以下混用是**良性**的（A 类），不需要拆分：
- 文件中定义了 dataclass（`frozen=True`、无可变状态、仅作为函数的返回类型），同时包含操作这些 dataclass 的纯函数。例如 `compare.py` 中 `CompareResult`（frozen dataclass）和 `dict_compare_score()` 系列比较函数——它们天然内聚，拆开反而迫使读者在两个文件间跳转。
- 文件以纯函数集为主，其中夹杂的"类"本质是 typed tuple（`@dataclass(frozen=True, slots=True)` 的小型数据结构），不是有状态的服务对象。例如 `tensor_utils.py` 中 `OpMemoryProfile` 等 profiling 数据容器和大量张量工具函数。
- 多个同族类（如多个 scheduler 子类）共享同一个文件——它们有相同的职责，放在一起是合理的。

以下混用**需要拆分**（B 类）：
- 有状态的、有生命周期的服务类旁边有无关联的独立函数。例如一个 `RewardCalculator` 类（管理配置、缓存、状态）旁边有 `normalize_targets()`、`validate_tool_calls()` 等不依赖 `self` 的函数——它们应该提取到 `helpers.py`。
- 复杂的状态管理类和纯计算函数混在一起，读者无法一眼判断函数的副作用范围。

**判断标准：** 读者能否一眼看出这个文件中的函数是否有副作用？如果看到文件里有类，不确定函数是否依赖类的状态——那就是 B 类，该拆。

### 8.5 输出目录隔离

源码和数据是资产，运行产物是消耗品。两者必须物理隔离——输出目录不在源码树内，由配置文件的 `output` 字段指定。默认值指向项目根目录下的 `outputs/`，这个目录在 `.gitignore` 中排除。

```
outputs/<run_name>/
├── config.yaml           # 本次运行的配置备份（完整复现）
├── checkpoints/          # 模型 checkpoint
├── logs/                 # 按功能域分文件的日志
└── results/              # 最终产出（数据集、模型、评测结果）
```

每次运行自动生成唯一的 `run_name`（默认基于时间戳），防止误覆盖。运行结束后，用户可以根据 `outputs/` 下的 `config.yaml` 完整复现本次运行——不需要回头找原始配置文件。

### 8.6 导入与文件头

统一使用绝对导入，禁止相对导入。`from __future__ import annotations`（PEP 563）仅在出现循环导入导致类型注解无法求值时使用，不要求逐文件添加。在 Python 3.11+ 项目中，`str | None` 语法已原生支持，`TYPE_CHECKING` 守卫已能处理大多数循环导入场景，无需在每份文件顶部添加样板代码。`__init__.py` 负责 re-export 公开 API，让外部使用者不需要关心内部文件结构。

### 8.7 版本号：单一来源

项目的版本号**由 git tag 通过 `setuptools-scm` 自动推导**，`pyproject.toml` 中声明 `dynamic = ["version"]`。这是宪法 1.4（单一真相源）和 2.1（防呆设计）的加强体现：

- `pyproject.toml` 的 `[tool.setuptools_scm]` 配置版本推导规则（tag 格式、前缀等），这是版本机制的**唯一配置来源**
- 实际的版本号字符串不在任何文件中——它从 `git describe --tags` 推导，发版时 `git tag vX.Y.Z` 即可
- **禁止**在 `__init__.py` 中定义 `__version__` —— 事实上有两个真相源
- **禁止**在 `config_example.yaml` 注释中写版本号 —— 模板文件是给用户看的，不是版本记录
- **禁止**在代码中硬编码版本号字符串

运行时通过 `importlib.metadata.version("package-name")` 获取版本号。用户查看版本：`git tag --sort=-v:refname | head -1` 或 `pip show package-name`。

**为什么不用 `pyproject.toml` 的 `version` 字段？** 因为 `pyproject.toml` 同时承载了版本号和依赖声明两个完全不相关的信息。如果版本号硬编码在 `pyproject.toml` 中，Docker 构建时改版本号会导致文件哈希变更，依赖层缓存失效，需要重新下载所有包。`setuptools-scm` + `dynamic = ["version"]` 将版本号从文件中解耦到 git tag 中——`pyproject.toml` 不包含版本号字符串，版本号变更不改变文件哈希，因此 `pyproject.toml` 可以安全地放入依赖层，不会触发依赖重建。版本号变更仅影响源码层（`COPY src`），而依赖层缓存完全复用。这是 §1.4（单一真相源）和 §14.3（Docker 层缓存）协同作用的结果。

**判断标准：** 发版需要改几个文件？如果超过 0 个，违反本条款。

- **禁止**在源代码文件中嵌入宪法版本引用（`BADGE Constitution vX.Y`、`constitution vX.Y §Z.W`
  或类似模式）。宪法版本号只在宪法仓库本身中维护。将版本引用散布在项目源代码中会导致漂移，
  违反了与 §8.7 项目版本号相同的单一真相源原则。宪法仓库自身的 `CLAUDE.md` 和 `tools/`
  脚本除外——它们是宪法内容的一部分，不是项目代码。

---

## 九、数据与接口

### 9.1 数据模型：pydantic

所有核心数据结构使用 pydantic `BaseModel` 定义。禁止裸 `dict` 或 `list` 传递业务数据。`extra = "forbid"` 拒绝未知字段。嵌套结构用嵌套 pydantic 模型，不做 dict 套 dict。

**模型纯净性：** 数据模型遵循与模块边界（§1.1）相同的一事一责原则。一个模型只包含属于它概念范畴的数据。当需要附加不相关的元数据时，不向现有模型添加字段——建一个新模型，用组合（嵌套）代替污染。例如，observation 应保持干净，只包含观测数据本身；额外的控制标记（如 reset_task_done）应放在 wrapper 模型中，以 observation 作为其字段之一。

`frozen = True` 建议用于从外部数据源加载后不再被修改的纯数据对象（如训练样本、评分结果），它可以防止意外修改。但对于配置对象等需要在初始化阶段被 CLI 参数覆盖的场景，允许不使用 `frozen`；此时应确保初始化完成后不再修改配置对象。

### 9.2 类继承：ABC 模板方法

有多套实现时，ABC 基类定义流程骨架（公开方法），每一步调用 `_` 前缀的私有钩子。具体子类只覆盖钩子，不覆盖公开方法。基类提供钩子的默认实现，减少子类重复代码。扩展新实现时，定义新类并注册，不修改任何现有代码。

### 9.3 注册表：动态扩展

多个同类型实现通过注册表（字符串到类的映射）管理。运行时按名称查找并实例化。查找失败时给出明确的错误信息，列出所有可用选项。新增实现只需一行注册代码。

---

## 十、CLI 设计

### 10.1 配置驱动命令

**原则：config 文件是运行产物的唯一描述。** 运行产物包括一切落盘输出（模型权重、数据集、评测结果、日志与记录文件），落盘位置是产物的一部分（output_dir、run_name 均由 config 决定）；输出到外部系统的内容同样视为产物。唯一例外：直接打印到 stdout 且不落盘的内容不是产物。

**复现定义：** 同一 config + 同一 seed 下，排除不可控随机性（机器负载导致的耗时差异、GPU 内核选择等无法控制的差异）后，落盘输出一致，即为复现（§6）。复现是对参数归属判断的基准：**仅凭输出目录备份的 config.yaml（§8.5）能否复现本次运行的全部落盘输出，是判断参数归属的唯一标准。**

**推论 1：凡参与决定产物的参数必须在 config 文件中。** 否则同一 config 会因 CLI 参数不同而产出不同结果——config 与产物的一一对应被打破，§8.5"备份 config.yaml 即可复现"的承诺失效。

**推论 2：CLI 参数与 config 字段零交集。** 一个参数只能有一个真相源（§1.4）。不允许"config 里有值、CLI 再覆盖"的机制——覆盖即双真相源。当新增命令代表本质上不同的生命周期操作（训练 vs 导出）、无法自然地表达为配置开关时，新增命令是允许的，但命令本身同样受本节约束。

**CLI 参数三原则（适用于所有 CLI 命令）：**
1. **输入定位参数**：指定输入文件或数据——定位输入，不是配置内容。最典型的例子是 `--config`，其他如 `--data`、`--limit`、`--checkpoint` 需根据项目语义判断是否属于此类别；
2. **运行环境参数**：不落盘、不进入产物，只影响"在哪里跑"（如 `--gpus`、`--master_port`）；
3. **运行边界参数**：不产生完整运行产物（如 `--smoke` 冒烟验证，等价于 max_steps=1 的 config，不改变训练语义）。

**`--config` 是唯一明确列出的参数名**——其他参数名因项目而异，必须根据以上三原则逐项审核：同一 config + 同一 seed 下，该参数取不同值是否导致落盘输出变化？若是，则必须进 config。

**判断标准（技法）：** 对任意参数 X——同一 config + 同一 seed 下，X 取不同值若导致任何落盘输出的存在性、位置或可控内容不同，则 X 必须进 config。

**输出约束（技法）：** 一切 CLI 命令的落盘输出由 config 决定——禁止"输出定位"参数（`--output`、`--output-dir` 等）。工具命令（validate/evaluate/analyze 等，不参与训练产物）同样受约束，三选一：只 print 不落盘；接受 `--config` 且输出写入 config 决定的目录；降级为 scripts/ 脚本（§10.2）。

**防呆（技法）：**
- CLI 参数在代码中唯一枚举，测试锁定——新增 CLI 参数必须通过"不落盘"测试（同一 config 两次运行，仅 X 不同，断言落盘输出一致）
- 日志与记录文件不得包含运行环境元数据（GPU ID、宿主机路径、容器名）——它们不可复现且不属于产物
- 禁止自定义环境变量承载任何配置或路径参数（§7.1）：仅允许 CUDA/NCCL/PyTorch 标准基础设施变量；宿主路径注入走 CLI 参数（如 run.sh `--model-dir`）

### 10.2 CLI 与 scripts 的边界

**CLI 是面向用户的稳定接口，scripts 是面向开发者的临时工具。** CLI 的参数和输出格式是契约，不能随意改。scripts 是一次性的、实验性的、随时可以删的，不需要考虑向后兼容。

**制度化判据：** 脚本出现以下任一情况即成为制度化接口，必须升级为 CLI 子命令或删除：
- 被测试固化（tests/ 中存在对应测试）
- 被 README 或 docs/ 引用
- 被工作流反复使用、使用范围超出了开发者本人

scripts/ 只保留一次性、实验性、未制度化的工具。升级为 CLI 子命令的脚本同时受 §10.1 约束（输入定位参数 + config 决定输出）。

---

## 十一、测试

### 11.1 工具链

pytest 运行测试，ruff 格式化 + lint（`ruff format` + `ruff check --fix`），mypy 类型检查。三者配置在 `pyproject.toml` 中，版本锁定在 dev 依赖中。

### 11.2 目录结构

`tests/` 的目录树镜像 `src/` 的目录树。找测试文件不需要思考：`src/project/core/schema.py` 的测试就在 `tests/core/test_schema.py`。端到端测试放在 `tests/e2e/` 独立目录下。

### 11.3 测试规范

单元测试覆盖公开接口，外部依赖用 mock 替代。端到端测试覆盖完整 pipeline。参数化测试覆盖边界条件。测试函数命名描述三个要素：做了什么、什么条件下、期望什么结果。每个测试独立，不依赖执行顺序。

---

## 十二、代码风格

### 12.1 类型标注

联合类型用 `str | None`，不用 `Optional[str]`。`py.typed` 标记文件。行宽 100，目标 Python 3.11+。`from __future__ import annotations` 仅在出现循环导入导致类型注解无法求值时使用（见 8.6）。

### 12.2 命名

类 PascalCase，函数和变量 snake_case，私有成员 `_` 前缀，常量 UPPER_SNAKE_CASE，类型别名 PascalCase，模块文件 snake_case。

**命名即文档：** 文件名是代码库的第一行文档。通过目录层级和文件名，读者应能推断文件的作用、所属模块和继承关系。文件名不是给自己看的标签，是给未来读者（包括 AI agent）的导航信号。当文件需要被重命名才能让新读者理解其职责时，说明原来的命名是失败的。

**类名与路径互映：** 类名应编码两个信息——所属**域**与**功能**。域在路径中必有一级目录与之对应，功能在路径中必有一级文件与之对应。`Qwen35Adapter` 应位于 `.../qwen35_36/adapter.py`，而不是散落在 `utils.py` 里。由此推出机械规则：**类名应能分解为路径分量**——类名的 CamelCase 词依次对应包名 → 目录名 → 文件名（snake_case），每层级一个词。`GraspoFlowTrainer` → `graspo/flow/trainer/trainer.py`（`Graspo`=包、`Flow`=目录、`Trainer`=文件），`GRASPORippleLoss` → `graspo/ripple/loss.py`（`Ripple`=目录、`Loss`=文件）。**目录层级已提供域上下文，文件名只需编码功能部分，无需将全类名拍平进文件名**——`graspo_flow_trainer.py` 或 `graspo_ripple_loss.py` 是冗余的，它把路径信息重复了一遍。豁免：类目录（§8.3）中的文件按功能域命名，目录名承载类名的定位；混入类（`_` 前缀或 `*Mixin` 后缀）按功能命名（`generation.py` 中的 `_Qwen35GenerationMethods`、`checkpoint.py` 中的 `CheckpointMixin`）；同族多类文件（§8.4）按功能族命名（`ops.py` 中的多个 `Qwen35*StageOp`）；数据容器类型（frozen dataclass、pydantic 模型）依附于操作它们的函数族，按函数族命名（`compare.py` 中的 `CompareResult`）；测试文件按被测模块命名（§11.2），其中的测试辅助类不适用本条。

**为什么只约束类，不约束函数？** 函数由所在文件提供域上下文，定位责任在文件；类是跨文件引用的自定位单元，且可能升级为目录（§8.3）——类名必须自带定位信息。

**判断标准：** 给定一个类名，能否不看代码就说出它应位于哪个路径？给定一个路径，能否说出其中应有哪些类族？双向都答得上，命名才合格。

### 12.3 语言规范

`README.md` 和 `README.zh-CN.md` 使用中英双语——项目的门面要同时覆盖英文和中文两大开发社区，这是当前开源世界最活跃的两个语系。

所有其他文件——注释、docstring、架构文档、配置注释——**中文或英文任选其一，作者自行决定**。不允许使用其他语言。单文件内保持语言基本一致，少量技术术语、代码引用或结构性分隔符可保留英文原文。

**配置模板注释**（`config_example.yaml`）默认使用**英文**——配置模板是面向用户的界面，英文是全球开发者社区的通用语言。

**提交信息**使用英文（见 19.1）。

**技术术语、算法名称、框架名称、学术概念**保留英文原文。例如 Flink、1F1B、KV cache、attention mask、backpressure —— 这些术语的英文原文是领域内工程师的共同语言，强行翻译反而损失信息密度。原则是：**以准确性优先，不以语言纯洁性牺牲技术表达。**

注释写 **why**，不写 **what**。禁止无用注释。

### 12.4 文件头注释：职责声明

每个源码文件开头必须用注释声明：本文件的功能与职责范围，一句话说清。这是给 AI（也是给人）的边界声明——当 AI 想往文件里塞不相关的方法或类时，文件头是第一道防线（防呆设计，见 §2）。职责无法用一句话说清 → 文件该拆（§1.1）。

- 文件头可以是模块 docstring 或注释块，语言遵循 §12.3
- `__init__.py` 豁免——它的职责就是 re-export 公开 API（§8.6），无需重复声明
- 职责声明属于导航与防呆信息，不是 §12.3 禁止的"无用注释"——它回答"这个文件为什么存在、什么内容该放进来"

**判断标准：** 文件头的职责声明能否一句话说清"这段代码该不该放进这个文件"？如果不能，要么注释没写清楚，要么文件边界已经失守。

---

## 十三、错误处理与日志

### 13.1 异常：不吞

**绝对禁止** `except Exception: pass` 或裸 `except: pass`。每一个 `except` 块必须包含明确的处理逻辑（重试、降级、转换为用户可读信息后退出）。异常原则上在边界层统一处理，在内部层尽早暴露。

### 13.2 日志分级

DEBUG 给开发者排查 bug，INFO 给用户了解运行状态，WARNING 给用户评估运行质量，ERROR 告诉用户为什么失败。

**每个项目必须提供至少一个标准 Python `logging` 通道**，使用这四种级别。这确保与日志聚合工具、CI 流水线和调试工作流兼容。在这个必需通道之外，项目可以增加领域特定的日志文件（结构化 JSONL 用于指标、时序事件日志、原始 rollout 记录等）——这些属于系统个性化扩展，不受四级约束。

### 13.3 日志分文件

日志文件的拆分粒度与模块边界（§1.1）一致：**互相独立的模块分别记录日志，紧密耦合的流水线步骤共享同一日志文件。** 每个独立的日志域区分**易读日志**（人类可读，INFO 级别）和**原始日志**（机器可解析，DEBUG 全量）。ERROR 汇聚到公共错误日志文件。所有日志文件位于输出目录下的 `logs/` 子目录中（见 8.5 输出目录隔离）。

具体的文件名和模块划分由各项目根据自身架构决定。例如：
- 训练框架中，rollout 生成、奖励计算、策略优化是同一训练循环的紧密耦合步骤——它们共享一个 `training.log`，而不是各写各的
- 数据管道中，如果抽取、转换、加载由三个独立服务执行（各自可独立部署和测试），则可以分别记录日志
- 一个项目中同时存在训练模块和 API 服务模块——两者互相独立，各自有独立的日志文件

**核心原则：拆分是为了让排查者不需要同时打开无关模块的日志，但也不能让排查者在追踪一个因果链时被迫跨文件拼接时间线。** 结构化的领域特定日志（如 JSONL rollout 记录、时序事件日志）属于系统个性化扩展（见 13.2），不受此约束——它们面向机器分析，不是面向人类排查。

**判断标准：** 排查一个事件时，需要同时打开几个日志文件跨文件关联时间线？如果需要反复跳转，说明拆分过细，应该合并。

### 13.4 调试：简单即可靠

**原则：** 调试手段的复杂度不应超过被调试系统的复杂度。调试工具本身也是代码，也会出 bug。当复杂的日志框架、分布式追踪、异步采样器本身成为故障源时，回退到最简单的可靠手段。

**技法：** `print()` + stdout 重定向是最后且最可靠的退路。这不是倒退，是退路设计（§3.1）在调试领域的应用——降级到更简单但更可靠的方案，保证调试信息不丢失。

**判断标准：** 设置调试环境需要多长时间？如果调试环境本身比被调试的 bug 更复杂，说明调试手段出了问题。

---

## 十四、依赖与部署

### 14.1 许可证约束

所有依赖包必须是 MIT、Apache 2.0 或等价的可商用宽松许可证。禁止 GPL、AGPL 等传染性许可证。引入新依赖前检查许可证兼容性。

### 14.2 依赖管理：uv

所有项目使用 `uv` 作为唯一包管理器。`.python-version` 锁定 Python 版本。`pyproject.toml` 声明依赖约束（包名 + 版本范围），`uv.lock` 由 `pyproject.toml` **通过 `uv lock` 生成**，记录完整解析的依赖图及精确版本和内容哈希。两者共同构成依赖的唯一真相源——不存在其他依赖声明。两个文件均提交到 git。`uv sync` 一键搭建开发环境。

**禁止手动编辑 `uv.lock`。** 它是生成产物。依赖变更流程：`pyproject.toml` → `uv lock` → `uv.lock`。不设 `requirements.txt`、`Pipfile`、环境变量覆盖依赖版本。

### 14.3 Docker 必须支持

每个项目必须提供 Docker 部署。Base image 锁定具体版本（不用 `latest`）；强烈建议锁定 SHA256 digest。Dockerfile 两层构建（依赖层 + 源码层），利用层缓存。依赖安装使用 `uv sync` 或 `uv pip install` 配合 `uv.lock`，确保容器内依赖版本与开发环境一致。`build.sh` 封装构建命令。

**Docker 层缓存设计：** 依赖层同时 COPY `uv.lock` 和 `pyproject.toml`。版本号由 `setuptools-scm` 从 git tag 推导（§8.7），`pyproject.toml` 中 `dynamic = ["version"]` 不包含版本号字符串，因此版本号变更不改变 `pyproject.toml` 的文件哈希。依赖层只在 `uv.lock` 或 `pyproject.toml` 真正变更时才重建——日常的版本号发布和代码修改都不会触发依赖层重建。

**BuildKit cache mount：** 所有 `uv sync` 和 `uv pip install` 命令使用 `RUN --mount=type=cache,target=/root/.cache/uv`。BuildKit 在构建之间持久化 `/root/.cache/uv` 目录，即使依赖层被重建（如 `uv.lock` 变更），包也从本地缓存读取，不走网络。这是 Docker 官方推荐模式，所有现代 Dockerfile 应标配。

**Docker 构建中的版本号：** `build.sh` 通过 `git describe --tags` 获取版本号，通过 `--build-arg VERSION=X.Y.Z` 传入 Dockerfile，Dockerfile 设置 `SETUPTOOLS_SCM_PRETEND_VERSION=${VERSION}`。这确保 shallow clone、CI 环境、COPY-only 上下文（无 `.git` 目录）中也能正确构建，同时保持版本号单一来源。

**推荐 Dockerfile 模板：**

```dockerfile
# Layer 1: dependencies (cached when uv.lock AND pyproject.toml unchanged)
# pyproject.toml IS copied here — version is managed by setuptools-scm
# (dynamic = ["version"]), so version bumps don't change this file's hash
# and don't invalidate this layer.
COPY uv.lock pyproject.toml .python-version README.md LICENSE ./
RUN --mount=type=cache,target=/root/.cache/uv \
    uv sync --no-install-project --frozen --no-dev

# Layer 2: source code
COPY src ./src
COPY configs ./configs
COPY data ./data

ARG VERSION=0.0.0
ENV SETUPTOOLS_SCM_PRETEND_VERSION=${VERSION}

RUN --mount=type=cache,target=/root/.cache/uv \
    uv sync --frozen --no-dev
```

**推荐 build.sh 模板：**

```bash
VERSION=$(git describe --tags --abbrev=0 2>/dev/null || echo "0.0.0")
IMAGE_NAME="${IMAGE_NAME:-project:${VERSION}}"
docker build --build-arg VERSION="${VERSION}" -t "${IMAGE_NAME}" -f docker/Dockerfile .
```

- **build.sh 版本：** 版本号必须通过 `git describe --tags` 获取，不得硬编码。`IMAGE_NAME` 环境变量覆盖仍可用于手动测试，但默认值必须来自 git tag。

---

# 第三层：安全与项目治理

## 十五、安全与机密

安全不是在发布前才想起来加上的补丁，而是约束每一次提交的设计前提。原则很简单：**如果你不想它出现在 GitHub 上，它就不该出现在任何已跟踪文件中。**

### 15.1 提交前检查

每次提交前，以下检查必须全部通过。这些检查应集成到 CI 或 pre-commit hook 中，由配套的 `tools/` 脚本自动执行，而非依赖人工记忆。宪法定义检查类别，脚本定义具体匹配模式。

**1. 机密扫描：** 已跟踪的所有文件中不得出现任何机密信息。机密包括：
- 任何形式的密钥、密码、token、私钥
- 内网 IP 地址、内网域名、内网路径
- 任何你不想在开源后出现在 GitHub 上的信息

**2. 文件类型检查：** 以下内容不得出现在已跟踪文件中：
- `.env` 文件（仅 `.env-example` 可提交）
- 构建产物、缓存、虚拟环境
- IDE 配置文件
- AI 助手文件（`CLAUDE.md`、`AGENTS.md`、`CLAUDE.zh-CN.md`）
- 临时本地文件——所有临时文件必须在 `.local/` 中（见 §XVI）

**3. 内容检查（人工 + AI 辅助）：**
- 无训练数据、用户数据、私有数据集
- README 中无内部资源引用

### 15.2 机密管理原则

机密信息的唯一定义来源是 `.env` 文件（本地，不入库）。（如果项目没有非基础设施环境变量——基础设施型项目的定义见 §19.2——则 `.env` 和 `.env-example` 均不需要。）任何代码、文档、配置模板、示例文件中不得出现机密信息的字面值。如果某条信息你不想在开源后出现在 GitHub 上，它就不应该出现在任何已跟踪的文件中，没有例外。

### 15.3 泄露后处理

如果机密信息曾被提交到 git 历史中：
- 确认 `git log --all --full-history` 中无敏感文件历史
- 使用 `BFG Repo-Cleaner` 彻底清除所有痕迹，轮换所有已泄露密钥
- 确认 `LICENSE` 文件存在且正确
- 确认 README 中无内部资源引用

---

## 十六、临时与本地文件

每个项目都会产生一些开发过程中有用但不属于永久代码库的文件——运行日志、迁移计划、部署笔记、个人实验。如果没有指定归宿，这些文件会散落在仓库各处，迟早有一份带着内网 IP 或密码被提交进去。

### 16.1 `.local/` — 临时文件的唯一归宿

项目根目录下的 `.local/` 是所有临时文件的**唯一**允许存放位置。该目录在 `.gitignore` 中整体排除，其中的任何内容都不会被提交到代码库。

`.local/` 不是"建议放这里"，而是"只能放这里"。任何临时文件如果出现在 `.local/` 之外的已跟踪路径中，即构成违规。

### 16.2 什么属于 `.local/`

满足以下任一条件的文件属于临时文件，必须放入 `.local/`：
- 包含运行环境信息（IP、主机名、容器名、SSH 用户、内网路径）
- 描述一次性操作（迁移计划、部署记录、实验跟踪）
- 个人笔记、调试记录、运行监控
- 临时数据、实验配置
- 任何不属于永久代码库的工程文件

如果某文件对项目有长期价值，应转化为正式文档放入 `docs/`。如果仅对个人或当前阶段有短期价值，放入 `.local/`。

**唯一的例外：工作日志（§17.5）。** 它有长期价值，但必然包含环境信息（工作环境与工具说明），因此只能存在于 `.local/`，不得转化为 `docs/` 文档——这是数据边界（§1.4）的一部分：环境信息不允许进入已跟踪文件。

### 16.3 与 `scripts/` 的关系

§10.2 定义了 `scripts/` 是共享临时代码工具的目录。两者分工：
- `scripts/` — 共享的临时代码工具（入 git，不承担向后兼容义务）
- `.local/` — 私有的临时文件（不入 git，不共享，不承担任何义务）

---

## 十七、文档体系

### 17.1 双语文档

`README.md`（英文）和 `README.zh-CN.md`（中文）必须**内容镜像**，不仅仅是结构镜像。同一段操作指引、配置说明、FAQ 条目在两个版本中必须同时存在，不得出现一个版本有而另一个版本没有的内容。每次版本发布时，两个 README 必须同步更新。

章节结构：简介 → 快速开始 → 数据格式 → 配置参考 → 输出说明 → 开发指南 → FAQ。

### 17.2 架构文档

`docs/` 目录下按主题拆分，每个文件聚焦一个关注点。文档的内容边界由 §1.6 定义：**写模块边界、职责、接口，写结构级的"为什么"**——实现细节在代码注释里（§12.4），不在文档里。

**文档中的图一律用 Mermaid 绘制，禁止 ASCII 手绘图。** Mermaid 机器可解析、GitHub/IDE 原生渲染、AI 可生成可修改；ASCII 图无法维护，AI 也无法可靠地重绘。节点名称一律用英文双引号包裹（`"Training Loop"`），保证各渲染器的兼容性。架构图、流程图、数据流图、泳道图是架构文档的必备要素——看完了就知道系统是如何运作的。目录树、表格等纯文本结构不属于"图"，不受本条约束。

**`docs/` 是项目级架构文档，不包含特定部署的实验记录。** 部署笔记、实验记录、运行监控等属于 §16.2 定义的临时文件，应放入 `.local/`。

**判断标准：** 一个读者（人或 AI）看完 `docs/` 后，能否说出系统的模块边界、数据流向和关键流程？能否说出某个类为什么这样设计？如果前者能、后者的答案在代码注释里而非文档里，文档与注释的边界就是对的。

### 17.3 AI 助手文档

如果项目使用 AI 编程助手（如 Claude Code、GitHub Copilot），可以按需生成 `CLAUDE.md` 和 `AGENTS.md` 供助手使用——信息密度高、格式结构化，帮助 AI 理解项目的架构和规范。这些文件**不入 git 版本控制**（在 `.gitignore` 中排除），属于本地开发环境的一部分。如果项目不使用 AI 助手，不需要创建这些文件。

使用工作日志的项目（§17.5），`CLAUDE.md` 中应放置一行指向 `work_log_current.md` 的指针，让 AI 每次会话都能发现日志入口。

### 17.4 版本记录

项目不维护独立的 Changelog 文件。Git 提交记录是唯一的、权威的变更历史。版本发布时用 `git tag` 标注版本号，用 `git log` 查看变更。提交信息应遵循 §19.1 的格式规范，确保 `git log --oneline` 即可阅读项目历史。

### 17.5 工作日志：持续交接

交接不是一个事件，是一个持续状态。与其在每次交接时写一份压缩的交接文档，不如维护一份工作日志——它本质上就是持续更新的交接文档，让任何接手者（人或 AI）随时可以继续工作，无需询问原作者。

工作日志分为两个文件，放在 `.local/` 下，不入 git（必然包含环境信息，§16.2）：

- **`work_log_current.md` —— 状态区，每次会话必读。** 只保存最新值，原地更新，永不修剪：当前进展、下一步任务、当前主要难点与问题、计划尝试的解决方案、工作环境与工具说明。建议保持在一百行以内——它承担"30 秒了解现状"的职责。
- **`work_log_history.md` —— 事件区，按需阅读。** 按时间戳增量追加（条目以 `YYYY-MM-DD` 开头）已完成的任务和已解决的问题，新条目追加在末尾。日志过长时做选择性精简：从头部（最旧的）删除**已沉淀进代码或文档、不再有参考价值**的条目；重要的决策上下文可以永久保留。**修剪是取舍，不是截断**——如果精简后接手者无法比读 git log 更快地理解现状，就是剪过头了。

与 git 的关系：git 提交记录是权威的变更事实（不可改写，§17.4），工作日志是叙事的决策上下文（可选择性压缩）。日志记录 git 里没有的东西——为什么、难点、方案、下一步。日志不是 Changelog 的替代品，不承担权威性，其中的每条陈述应能被 git 或代码交叉验证。

**更新时机：** 每次工作会话结束或阶段性任务完成时更新——事件区追加 3~5 行，状态区刷新当前值。使用 AI 助手的项目，`CLAUDE.md` 中放置指向 `work_log_current.md` 的指针（§17.3）。

**判断标准：** 一个新人（或新 AI）只读工作日志与代码库，能否完全继续工作而不需要询问原作者？如果不能，日志不完整。

---

## 十八、版本演进与债务管理

技术负债是工程质量的癌症。今天偷的懒，明天变成两倍工作量，后天变成谁也不敢碰的禁区。核心原则只有一条：**新架构验证通过之后，旧架构必须根除。**

### 18.1 不留负债

- 新架构上线、测试通过、稳定运行后，旧代码**立即删除**，不保留"兼容模式"，不保留 `legacy/` 目录，不保留 `# TODO: remove after v2` 注释。代码库中只存在一套当前架构。
- 删除旧代码的同时，同步更新：命名（不再有 `v2`、`new`、`legacy` 前后缀）、配置模板（`config_example.yaml` 反映当前架构）、文档（README 和 docs/ 中的架构描述）、测试（删除旧架构的测试，不保留"以防万一"）。
- 版本号在每次发布时更新，遵循 `MAJOR.MINOR.PATCH`：架构重构、不兼容的配置变更升 MAJOR，新增功能升 MINOR，bug 修复升 PATCH。

### 18.2 旧系统迁移

当旧版本用户需要升级到新架构时，提供一键迁移方案：

- 迁移脚本放在 `scripts/` 目录下，命名格式：`migrate_v1_to_v2.py`、`migrate_config_v2_to_v3.sh`
- 脚本头部注释注明：源版本号 → 目标版本号、迁移了什么（配置文件格式、checkpoint 格式、数据格式）、不可自动迁移的部分（需要用户手动处理的）
- 迁移脚本是临时工具——只服务于当前这次大版本升级，下一个大版本时可以被新的迁移脚本替代。旧迁移脚本保留在 `scripts/` 中供历史参考，因为可能有用户跳版本升级

**判断标准：** 一个新人 clone 项目后，能看到旧架构的痕迹吗？如果能——遗留代码、过时注释、未更新的配置模板——版本管理就是失败的。

---

## 十九、开源管理

### 19.1 Git 工作流

- 禁止直接提交到 `main` / `master` 分支
- 所有开发在功能分支上：`feature/<描述>`、`fix/<描述>`、`docs/<描述>`
- 合并到 main 必须通过 PR，至少自己 review 一遍 diff
- 提交信息**建议**使用英文，格式：`type: short description`（feat、fix、docs、refactor、test、chore）。英文是开源社区的事实标准，`git log --oneline` 工具链以英文为第一语言，便于国际贡献者理解。主要贡献者社区使用其他语言（如中文）的项目可使用该语言，但应在一个仓库内保持一致。
- **历史连贯优先于回溯修正。** 已推送至公共仓库的历史提交**不得**回溯修改提交信息语言——修改已推送的历史会破坏每个协作者的本地 clone。规范从当前提交开始执行。例外：若历史提交包含泄露的机密信息，必须重写历史（见 §15.3）。
- 一个提交只做一件事

### 19.2 .gitignore 必须覆盖

- Python 运行时：`__pycache__/`、`*.pyc`
- 虚拟环境：`.venv/`、`venv/`
- 测试与类型检查缓存：`.pytest_cache/`、`.mypy_cache/`、`.ruff_cache/`
- 构建产物：`dist/`、`build/`、`*.egg-info/`
- 环境配置：`.env`（保留 `.env-example`；仅当项目有非基础设施环境变量时才需要；
  仅使用标准 CUDA/NCCL/PyTorch 分布式环境变量的项目——如 `CUDA_VISIBLE_DEVICES`、
  `NCCL_*`、`PYTORCH_*`、`RANK`、`LOCAL_RANK`、`WORLD_SIZE`、`MASTER_ADDR`、
  `MASTER_PORT`——可豁免）
- 输出与数据：`outputs/`、`data/`（示例数据除外）
- IDE：`.idea/`、`.vscode/`
- AI 助手：`CLAUDE.md`、`AGENTS.md`、`CLAUDE.zh-CN.md`
- 临时本地文件：`.local/`
- 系统文件：`.DS_Store`、`Thumbs.db`

### 19.3 开源协议

默认 MIT 协议。`LICENSE` 文件放在根目录。如果项目依赖了 Apache 2.0 协议的库，考虑使用 Apache 2.0 保持兼容。

---

## 二十、pyproject.toml 骨架

```toml
[project]
name = "my-project"
dynamic = ["version"]
description = "一句话描述"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
    "pydantic>=2.0",
]

[project.scripts]
my-project = "my_project.cli:main"

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.6", "mypy>=1.0"]

[build-system]
requires = ["setuptools>=75.0", "setuptools-scm>=8.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools_scm]
tag_regex = "^(?:v)?(?P<version>[0-9]+\\.[0-9]+\\.[0-9]+)$"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "BLE"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
```